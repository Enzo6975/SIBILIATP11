<UserControl
    x:Class="SIBILIATP11.UserControl.CreerCommande"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SIBILIATP11.UserControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid x:Name="grid" Background="White">
        <StackPanel x:Name="stackPanel">
            <StackPanel x:Name="stackPanel1"
                Height="60"
                Margin="20,0,20,0"
                VerticalAlignment="Top">
                <Label
                    Width="150"
                    Height="30"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Content="Rechercher :" />
                <TextBox Height="30" />
            </StackPanel>
            <StackPanel x:Name="stackPanel2"
                Height="30"
                Margin="0,10,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Orientation="Horizontal">
                <ComboBox Width="150" />
                <DatePicker Margin="30,0,0,0" />
                <Label Margin="30,0,0,0" Content="De" />
                <TextBox Width="50" Margin="5,0,0,0" />
                <Label Margin="5,0,0,0" Content="à" />
                <TextBox Width="50" Margin="5.5,0,0,0" />

            </StackPanel>

            <DataGrid
                x:Name="plats"
                Margin="30,30,30,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                IsReadOnly="True" Height="300" />
            
        </StackPanel>
    </Grid>
</UserControl>
